// Photos (importées → inlinées en base64 dans le build « fichier unique »).
import brocheMachine from './assets/broche-machine.jpg';
import kebab from './assets/kebab.jpg';

export const PHOTOS = {
  hero: kebab,             // accueil : le sandwich kebab (donne faim)
  broche: brocheMachine,   // section « Née du feu » (broche + panneaux chauffants)
  kebab,                   // produit kebab
};
